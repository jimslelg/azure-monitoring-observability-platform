# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl
*.tfplan
*.tfvars.local

# Sensitive values — environment tfvars in environments/ are intentionally tracked
# (they contain no secrets; secrets come from Key Vault / pipeline variables)

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local scratch
tmp/
